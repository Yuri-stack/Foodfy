{% extends "layouts/admin.njk" %} 

    {% block head %}
        <title>Chef {{ chef.name }}</title>
    {% endblock %}

    {% block container %}

        <section class="control">

            <div>
                <h1>Chef: {{ chef.name }}</h1>
            </div>

            <div>
                <a href="{{ chef.id }}/edit">Editar Chef</a>
            </div>

        </section>

        <section class="chef container">

            <section class="chef-principal">

                {% if not chef.file %}
                    <img class="img_circular" src="http://placehold.it/500x500?text=Chef sem foto">
                {% else %}
                    <img class="img_circular" src="{{ chef.file.src }}" alt="Imagem de {{ chef.name }}">
                {% endif %}

                <div class="chef-intro">
                    <span class="info-title">{{ chef.name }}</span>
                    <span class="info-subtitle">{{ chef.total_recipes }} receita(s)</span>
                </div>

            </section>

            <h1>Receitas</h1>

            <section class="recipes-admin container">

                {% if chef.recipes[0].id == null %}

                    Este Chef ainda não possui receitas.

                {% else %}

                    {% for item in chef.recipes %}

                        <div class="card-horizontal">

                            <img class="img_rounded"
                                src="{{ item.image }}" 
                                alt="{{ item.title }}">

                            <div class="card_info">
                                <span class="info_title">{{ item.title }}</span>
                                <span class="info_subtitle">por {{ chef.name }}</span>
                                <span class="info_view">
                                    <a href="/admin/recipes/{{ item.id }}">
                                        Visualizar
                                    </a>
                                </span>
                            </div>

                        </div>

                    {% endfor %}

                {% endif %}

            </section>

        </section>

    {% endblock %}